name: ATELIER 3 - Flask + ngrok (tout-en-un)

on:
  push:
    branches:
      - main  

jobs:
  flask-ngrok:
    runs-on: ubuntu-latest

    steps:
      - name: 🧰 Installer Python + pip
        run: sudo apt-get update && sudo apt-get install -y python3-pip

      - name: 🧪 Installer Flask + pyngrok
        run: pip3 install flask pyngrok

      - name: 🚀 Lancer Flask avec ngrok (120 sec)
        run: |
          echo "from flask import Flask
import threading, time
from pyngrok import ngrok

app = Flask(__name__)

@app.route('/')
def home():
    return '✅ Serveur Flask lancé via GitHub Actions + ngrok'

def run_ngrok():
    url = ngrok.connect(5000)
    print(f'🔗 Lien ngrok: {url.public_url}')
    time.sleep(120)
    ngrok.kill()

if __name__ == '__main__':
    threading.Thread(target=run_ngrok).start()
    app.run(host='0.0.0.0', port=5000)
" > app.py

          python3 app.py
